spring.jpa.generate-ddl=true
# Esta propiedad booleana indica si se har� algo con la base de datos al iniciar la aplicaci�n

spring.jpa.hibernate.ddl-auto=validate
# Con esto le diremos qu� hacer con la aplicaci�n al iniciar

# Existen m�ltiples opciones, validate s�lo hace un select para validar que en la base de datos existan
# los objetos configurados en el proyecto.

# Podemos crear tablas y secuencias asignando esto a create e hibernate se encargar� de sincronizar
# las entidades del proyecto con la base de datos.

# En este caso tenemos la base de datos creada, por ende s�lo vamos a validar.

spring.jpa.open-in-view=true
spring.jpa.show-sql=true
# Esto har� que todas las queries ejecutadas se muestren en el log
